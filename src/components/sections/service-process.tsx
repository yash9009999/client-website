import { Container } from "@/components/layout/container";
import { Section } from "@/components/layout/section";
const steps = [{ number: "01", title: "Tell Us What You Need", copy: "Start by outlining the property service you are looking for." }, { number: "02", title: "Discuss the Job", copy: "Share the relevant details so the requirements can be understood." }, { number: "03", title: "Receive Your Quote", copy: "We can discuss the next steps based on your requirement." }, { number: "04", title: "Arrange the Work", copy: "Agree a suitable way to move forward with the service." }];
export function ServiceProcess() { return <Section><Container><div className="max-w-2xl"><p className="eyebrow">How it works</p><h2 className="section-title">A straightforward way to get started</h2></div><ol className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{steps.map((step) => <li key={step.number} className="relative border-t-2 border-primary pt-5"><span className="text-sm font-bold tracking-[.16em] text-primary">{step.number}</span><h3 className="mt-4 text-lg font-bold tracking-tight">{step.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{step.copy}</p></li>)}</ol></Container></Section>; }
